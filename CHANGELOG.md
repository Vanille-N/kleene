# v0.1.0 -> v0.2.0

- Added safety guarantees by implementing closure and left recursion analyses
- Added ability to emit custom error messages
- New construct `hint` initiates a fork based on a prefix of the remaining text.
